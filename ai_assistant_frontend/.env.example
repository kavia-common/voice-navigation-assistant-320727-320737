# Optional: Base URL for the FastAPI backend (include scheme + host + port)
# Example (local): REACT_APP_BACKEND_URL=http://localhost:3001
# Example (Kavia): REACT_APP_BACKEND_URL=https://<your-backend-host>
REACT_APP_BACKEND_URL=
